apply plugin: 'android'

android {
    compileSdkVersion 18
    buildToolsVersion "19.0.2"
	compileOptions.encoding = "UTF-8"

    defaultConfig {
        minSdkVersion 10
        targetSdkVersion 19
    }
}

